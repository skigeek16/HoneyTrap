# --- Database Configuration ---
# Helper for async SQLite access. Defaults to a local file named honeytrap.db
DATABASE_URL=sqlite+aiosqlite:///./honeytrap.db

# --- LLM Configuration (Nebius AI) ---
# Your Nebius API Key. Required for intelligent, persona-based responses.
# Get one from: https://studio.nebius.ai/
LLM_API_KEY=your-nebius-api-key-here

# Base URL for the Nebius API (OpenAI-compatible)
LLM_API_URL=https://api.tokenfactory.nebius.com/v1/

# The specific model to use for generating responses. 
# Llama 3.3 70B is recommended for best instruction following.
LLM_MODEL=meta-llama/Llama-3.3-70B-Instruct-fast

# --- System Configuration ---
# Hardware device to run local ML models (Phishing/Intent detection).
# Options: 'cpu' or 'cuda' (if you have an NVIDIA GPU)
DEVICE=cpu

# --- Debugging ---
# Set to True to enable verbose logging
DEBUG=True